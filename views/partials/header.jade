// Common header
header
  nav
    li
      a(href='/') BrandUsed
    li
      a(href='/browse') Browse
    li
      a(href='/create') Create
    if user
      li
        a(href='/search') Search
    if user && user.isAdmin
      li
        a(href='/admin') Admin
    if user
      li
        a(href='/inbox')#navInbox Inbox
      li
        form(action='/logout', method='post')#logout-form
          a#button-logout Sign out
    else
      li
        a#button-login Sign in
      
include ./login.jade
